import { css } from "styled-components";

const colors = {
  Black: "#000000",
  Grey: "#EFEFEF",
  Red: "#ff0000",
  White: "#ffffff",
};

const fonts = {
  R_Content_1: css`
    font-size: 3rem;
  `,
};

const theme = {
  colors,
  fonts,
};
export default theme;
